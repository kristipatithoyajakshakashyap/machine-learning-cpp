# Theory

QDA allows a different Gaussian covariance for every class. Boundaries can curve, at the cost of estimating many more parameters. Small classes and redundant features make covariance estimates unstable; diagonal regularization improves numerical conditioning.
