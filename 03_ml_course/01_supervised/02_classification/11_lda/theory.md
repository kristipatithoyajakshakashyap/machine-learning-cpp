# Theory

LDA models each class as a Gaussian with its own mean and a common covariance. Bayes rule combines likelihoods and empirical class priors. The common quadratic term cancels, leaving linear decision boundaries. Strong covariance or Gaussian violations can damage calibration.
