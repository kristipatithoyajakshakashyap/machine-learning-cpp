# Theory

Epsilon SVR ignores residuals inside an epsilon-wide tube and penalizes distances outside it. C trades margin simplicity against fitting. Linear kernels give a linear function; RBF kernels compare local neighborhoods and require gamma tuning.
