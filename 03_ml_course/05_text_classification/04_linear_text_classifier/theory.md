# Theory

L2-regularized logistic regression learns a weighted combination of sparse TF-IDF features. Unlike Naive Bayes it models the conditional label distribution directly. Strong shrinkage can underfit rare spam indicators.
