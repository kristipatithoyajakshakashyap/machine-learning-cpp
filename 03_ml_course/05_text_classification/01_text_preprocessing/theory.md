# Theory

Tokenization converts variable strings to sequences of words. This course lowercases ASCII letters and splits on non-alphanumeric characters. Preserve original row IDs and remove exact duplicate messages before splitting. The final lesson shows how these choices affect a spam classifier.
