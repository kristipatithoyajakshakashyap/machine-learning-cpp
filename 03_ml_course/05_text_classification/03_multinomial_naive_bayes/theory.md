# Theory

The model assumes class-conditional independence of token occurrences. This simplifying assumption is rarely literally true, but often yields a strong text baseline. Laplace smoothing prevents a single unseen token from assigning zero likelihood.
